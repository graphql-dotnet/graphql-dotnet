---
title: Dependency Injection
order: 12
---

GraphQL .NET supports dependency injection through the `IServiceProvider` interface.
This is configured using `GraphQLBuilder` which is returned from `AddGraphQL()` extension method.

